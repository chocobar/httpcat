name: HTTP Cats for every HTTP code
description: Generate me a cat image for my HTTP code
assistants:
- name: HTTP Cat API
  apis:
    - name: HTTP Cat API
      description: Gets a cat picture for each HTTP Code
      url: https://http.cat
      schema: ./openapi/httpcat.yaml
